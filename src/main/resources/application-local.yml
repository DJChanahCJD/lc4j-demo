spring:
  application:
    name: lc4j-demo
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306?useInformationSchema=true&characterEncoding=utf-8
    username: root
    password: 123456
  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000
      jedis:
        pool:
          max-active: 8
          max-wait: -1
          max-idle: 8
          min-idle: 0
  
langchain4j:
  open-ai:
    chat-model:
      api-key: sk-ab245b77072143e288d44c2c52e69cf2
      model-name: qwen-max
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      log-requests: true
      log-responses: true
#  community:
#    dashscope:
#      chat-model:
#        model-name: qwen-max
#        api-key: sk-ab245b77072143e288d44c2c52e69cf2

server:
  port: 8901
  servlet:
    context-path: /api  # API前缀，文档地址 http://localhost:8901/api/doc.html || http://localhost:8901/api/v3/api-docs


# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.daque.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

logging:
  level:
    com.djchan.lcfjdemo.ai.config: DEBUG
    org.springframework.data.redis: DEBUG
    root: INFO